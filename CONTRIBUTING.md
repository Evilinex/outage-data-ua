# Керівництво для учасників

Дякуємо за інтерес до внеску в **outage-data-ua** ⚡
Проєкт автоматично отримує та нормалізує публічно доступні дані про планові відключення електроенергії в Україні і зберігає їх у форматі JSON.

---

## 🧭 Обсяг проєкту
- Репозиторій містить **скрипти Bash/Node.js** для отримання HTML, парсингу `DisconSchedule.fact` і публікації результатів.
- Мета — **структурований машиночитний JSON** у каталозі `data/`, який регулярно оновлюється пайплайном GitHub Actions.
- Усі внески мають поважати умови використання джерел і **не порушувати політик доступу**.

---

## 🔧 Локальний запуск та перевірка
Передумови: `Node.js 18+`, `jq`, `curl`.

1) Налаштуйте `.env` з мапою джерел (приклад у README): `REGION_SOURCES_JSON='{ "kyiv": "https://..." }'`.
2) Завантажте HTML:
```bash
scripts/fetch_regions.sh         # усі регіони з .env
scripts/fetch_regions.sh kyiv    # лише конкретний регіон
```
3) Розпарсіть у JSON:
```bash
scripts/parse_regions.sh         # парсинг усіх наявних outputs/*.html
REGION=kyiv scripts/parse_regions.sh  # лише конкретний регіон
```
Результат збережеться у `data/<region>.json`.

Політика помилок: якщо під час фетчу/парсингу стається помилка (наприклад, WAF/антибот), **попередні дані в JSON не видаляються**. Оновлюється лише `lastUpdateStatus` зі статусом `error`, кодом та повідомленням.

---

## 🧱 Формат даних і правила
- Кожен регіон — окремий файл: `data/<region>.json`.
- Базовий шаблон див. у `data/_template.json`.
- Поле `data` містить розклади по групах (наприклад, `GPV1.1`). Якщо можливо, інтервали конвертуються у ISO‑дату з часовою зоною `Europe/Kyiv` (`startLocal`, `endLocal`).
- Якщо структура джерела невідома, зберігається сирий фрагмент, а пояснення записується у `meta.dataEmptyReason`.
- Заборонено додавати персональні або чутливі дані.

---

## 🚀 CI/CD (GitHub Actions)
- Пайплайн запускається **кожні 15 хв** та вручну (`workflow_dispatch`).
- Етапи: Fetch → Parse → Commit & Push зміни у `main` (лише `data/*.json`).
- Коміти від імені `github-actions[bot]`. Для пушу потрібні `permissions: contents: write`. Якщо `main` захищена — дозвольте GitHub Actions пушити або використовуйте PR‑потік.

---

## 🧪 Внесення змін у код
1. Зробіть **fork**.
2. Створіть гілку: `feature/...` або `fix/...`.
3. Дотримуйтесь стилю коду (нижче) і перевірте локально (`fetch` → `parse`).
4. Відкрийте **Pull Request** з чітким описом змін і мотивацією.

> ⚠️ PR не прийматимуться, якщо пропонують обхід складних захистів, що порушують умови сайтів. Дозволено лише заголовки/налаштування, що імітують звичайний браузер.

---

## 🧩 Стиль коду
- Node.js 18+.
- Форматування — **Prettier**.
- Мінімум залежностей; пріоритет простим скриптам без важких фреймворків.
- Коментарі до нетривіальних місць (особливо парсинг та часові пояси).

---

## 🧾 Звітування про проблеми
Будь ласка, відкривайте [Issues](../../issues) і додавайте:
- регіон(и) і приклади;
- часову позначку (UTC та/або Europe/Kyiv);
- версію Node та ОС (для локальних проблем);
- фрагмент логів `fetch`/`parse` (без секретів).

---

## 🔒 Дані та юридичні застереження
- Дані збираються з **публічно доступних** вебсторінок.
- Учасники мають гарантувати, що не обходять механізми автентифікації й не порушують політик доступу.
- Репозиторій і мейнтенери **не претендують на право власності** на зібрані дані.
- Внески — за ліцензією **MIT** (див. LICENSE).

---

Дякуємо, що допомагаєте покращувати **outage-data-ua**! Кожен внесок робить набір даних кориснішим 💡
